<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Tree View -->
        <record id="view_expense_category_tree" model="ir.ui.view">
            <field name="name">personal.expense.category.tree</field>
            <field name="model">personal.expense.category</field>
            <field name="arch" type="xml">
                <tree string="Expense Categories">
                    <field name="name"/>
                    <field name="description"/>
                </tree>
            </field>
        </record>

        <!-- Form View -->
        <record id="view_expense_category_form" model="ir.ui.view">
            <field name="name">personal.expense.category.form</field>
            <field name="model">personal.expense.category</field>
            <field name="arch" type="xml">
                <form string="Expense Category">
                    <sheet>
                        <group>
                            <field name="name"/>
                            <field name="description"/>
                        </group>
                    </sheet>
                </form>
            </field>
        </record>

        <!-- Action -->
        <record id="action_expense_category" model="ir.actions.act_window">
            <field name="name">Expense Categories</field>
            <field name="res_model">personal.expense.category</field>
            <field name="view_type">form</field>
            <field name="view_mode">tree,form</field>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    Create your first expense category!
                </p>
            </field>
        </record>
    </data>
</odoo> 